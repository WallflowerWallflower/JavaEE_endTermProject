create table users (
    id int(11),
    name varchar(255),
    pass varchar(255),
    subscribe varchar(255),
    type varchar(255)
);

create table musics (
    id int(11),
    name varchar(255),
    yearOfPublish int(255),
    min varchar(255)
);
